<!-- FROM PATS2 FROM PROFESSOR LARRY HEIMANN -->
<div class="row">
  <div class="col s7">
    <div class="card">
      <div class="card-content">
        <span class="card-title">Login here</span>
          <br />
          <%= form_with url: sessions_path, local: true do |f| %>
          <div class="control-group">
            <%= f.label :email %>
            <div class="controls">
              <%= f.text_field :email %>
            </div>
          </div>

          <div class="control-group">
            <%= f.label :password %>
            <div class="controls">
              <%= f.password_field :password %>
            </div>
          </div>
          
          <div class="form-actions"><%= f.submit "Log In" %></div>
        <% end %>
      </div>
    </div>
  </div>
</div>